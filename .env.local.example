# MongoDB Atlas Configuration (REQUIRED)
# Get your connection string from MongoDB Atlas:
# 1. Go to https://cloud.mongodb.com
# 2. Click "Connect" on your cluster
# 3. Choose "Connect your application"
# 4. Copy the connection string and replace <password> with your actual password
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority

# Database name for the fraud detection system
MONGODB_DB_NAME=fraud_escalation

# Fireworks AI API Key (REQUIRED for agent LLM reasoning)
# Get your API key from https://fireworks.ai/account/api-keys
FIREWORKS_API_KEY=fw_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Voyage AI API Key (REQUIRED for embeddings & reranking)
# Get your API key from https://dash.voyageai.com/
# Used for: transaction embeddings, similarity search, document reranking
VOYAGE_API_KEY=pa-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# x402 Payment Secret (Mock secret for demo)
# For hackathon demo - use any random string
X402_PAYMENT_SECRET=demo_secret_key_12345

# Coinbase Developer Platform (REQUIRED for real CDP wallet payments)
# Get credentials from https://portal.cdp.coinbase.com/
# 1. Create an API key (download the JSON file)
# 2. Create a wallet on Base Sepolia
# 3. Fund with testnet USDC from Base Sepolia faucet
CDP_API_KEY_NAME=organizations/{org_id}/apiKeys/{key_id}
CDP_API_KEY_PRIVATE_KEY=-----BEGIN EC PRIVATE KEY-----\n...\n-----END EC PRIVATE KEY-----
CDP_WALLET_ID=wallet_id_from_cdp
CDP_NETWORK_ID=base-sepolia
CDP_RECIPIENT_ADDRESS=0x... # Signal provider wallet address

# Galileo Observability (OPTIONAL - only if you have time)
# Get credentials from https://rungalileo.io
# GALILEO_API_KEY=galileo_xxxxxxxxxxxxx
# GALILEO_PROJECT_ID=fraud-demo

# Environment
NODE_ENV=development
